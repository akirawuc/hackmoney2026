{
  "name": "agentflow-defi",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous AI-powered cross-chain DeFi agent platform",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @agentflow/web dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "keywords": [
    "defi",
    "ai-agent",
    "cross-chain",
    "yellow-network",
    "ethereum"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
